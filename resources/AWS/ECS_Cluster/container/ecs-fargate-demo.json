[
  {
    "cpu": ${cpu},
    "image": "${image}",
    "memory": ${memory},
    "name": "${name}",
    "networkMode": "${networkMode}",
    "portMappings": [
      {
        "containerPort": ${containerPort},
        "hostPort": ${hostPort}
      }
    ]
  }
]